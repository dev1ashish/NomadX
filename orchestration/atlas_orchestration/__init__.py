# Atlas Raman data-engineering orchestration layer.
# Software-defined assets that WRAP the existing atlas/* science code and the
# materialized data_cache/ outputs, attaching lineage + quality + contract
# metadata so Dagster renders the DAG, the data-quality funnel, and the
# data-scientist handoff contract.
